<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Record Rules -->
        <record model="ir.rule" id="subscription_plan_rule_user">
            <field name="name">Subscription Plan: User Access</field>
            <field name="model_id" ref="model_subscription_plan"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('membership_base.group_membership_user'))]"/>
        </record>

        <record model="ir.rule" id="subscription_subscription_rule_user">
            <field name="name">Subscription: User Access</field>
            <field name="model_id" ref="model_subscription_subscription"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('membership_base.group_membership_user'))]"/>
        </record>

    </data>
</odoo>
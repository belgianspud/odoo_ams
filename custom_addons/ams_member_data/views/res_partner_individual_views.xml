<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Enhanced Individual Member Form View -->
    <record id="view_partner_individual_form" model="ir.ui.view">
        <field name="name">res.partner.individual.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            
            <!-- Add Member ID and Status after name -->
            <field name="name" position="after">
                <field name="member_id" readonly="1" invisible="not is_member"/>
                <field name="is_member"/>
                <field name="membership_status" invisible="not is_member"/>
            </field>

            <!-- Add AMS membership section -->
            <field name="category_id" position="after">
                <group string="Membership Information" invisible="not is_member">
                    <group>
                        <field name="join_date"/>
                        <field name="member_since" readonly="1"/>
                        <field name="renewal_date"/>
                        <field name="paid_through_date"/>
                    </group>
                    <group>
                        <field name="membership_duration_days" readonly="1"/>
                        <field name="days_until_renewal" readonly="1"/>
                        <field name="is_renewal_due" readonly="1"/>
                    </group>
                </group>
            </field>

            <!-- Add engagement and contributions section -->
            <xpath expr="//group[@name='sale']" position="after">
                <group string="Engagement &amp; Contributions" invisible="not is_member">
                    <group>
                        <field name="engagement_score"/>
                        <field name="last_payment_date"/>
                        <field name="last_payment_amount"/>
                    </group>
                    <group>
                        <field name="total_contributions"/>
                        <field name="donor_level"/>
                    </group>
                </group>
            </xpath>

            <!-- Individual-specific fields -->
            <xpath expr="//group[@name='sale']" position="after">
                <group string="Individual Details" invisible="is_company">
                    <group>
                        <field name="date_of_birth"/>
                        <field name="gender"/>
                    </group>
                    <group>
                        <field name="credentials"/>
                    </group>
                </group>
            </xpath>

            <!-- System information tab -->
            <xpath expr="//notebook" position="inside">
                <page string="System Information" groups="base.group_system" invisible="is_company">
                    <group>
                        <group string="Integration">
                            <field name="legacy_contact_id"/>
                            <field name="ref" readonly="1"/>
                        </group>
                        <group string="Actions">
                            <button name="action_make_member" 
                                    string="Make Member" 
                                    type="object" 
                                    class="btn-primary"
                                    invisible="is_member"/>
                            <button name="action_renew_membership" 
                                    string="Renew Membership" 
                                    type="object" 
                                    class="btn-success"
                                    invisible="not is_member or membership_status not in ['active', 'grace', 'lapsed']"/>
                        </group>
                    </group>
                </page>
            </xpath>

        </field>
    </record>

    <!-- Individual Members Tree View -->
    <record id="view_partner_individual_tree" model="ir.ui.view">
        <field name="name">res.partner.individual.tree</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <tree string="Individual Members">
                <field name="member_id"/>
                <field name="name"/>
                <field name="membership_status"/>
                <field name="email"/>
                <field name="phone"/>
                <field name="mobile"/>
                <field name="city"/>
                <field name="state_id"/>
                <field name="renewal_date"/>
                <field name="is_renewal_due" widget="boolean"/>
            </tree>
        </field>
    </record>

    <!-- Individual Members Kanban View -->
    <record id="view_partner_individual_kanban" model="ir.ui.view">
        <field name="name">res.partner.individual.kanban</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <kanban string="Individual Members">
                <field name="member_id"/>
                <field name="name"/>
                <field name="membership_status"/>
                <field name="email"/>
                <field name="phone"/>
                <field name="mobile"/>
                <field name="renewal_date"/>
                <field name="is_renewal_due"/>
                <field name="image_128"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="o_kanban_image">
                                <img t-att-src="kanban_image('res.partner', 'image_128', record.id.raw_value)" alt="Member"/>
                            </div>
                            <div class="oe_kanban_details">
                                <strong class="o_kanban_record_title">
                                    <field name="name"/>
                                </strong>
                                <div t-if="record.member_id.value">
                                    <i class="fa fa-id-card" title="Member ID"/>
                                    <field name="member_id"/>
                                </div>
                                <div t-if="record.membership_status.value">
                                    <i class="fa fa-user" title="Status"/>
                                    <field name="membership_status"/>
                                </div>
                                <div t-if="record.email.value">
                                    <i class="fa fa-envelope" title="Email"/>
                                    <field name="email"/>
                                </div>
                                <div t-if="record.phone.value">
                                    <i class="fa fa-phone" title="Phone"/>
                                    <field name="phone"/>
                                </div>
                                <div t-if="record.is_renewal_due.value" class="text-danger">
                                    <i class="fa fa-exclamation-triangle" title="Renewal Due"/>
                                    Renewal Due
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Individual Members Search View -->
    <record id="view_partner_individual_search" model="ir.ui.view">
        <field name="name">res.partner.individual.search</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <search string="Search Individual Members">
                <field name="name" string="Name"/>
                <field name="member_id"/>
                <field name="email"/>
                <field name="phone"/>
                <field name="mobile"/>
                <field name="city"/>
                <field name="state_id"/>
                
                <filter string="Members Only" name="members_only" domain="[('is_member', '=', True)]"/>
                <filter string="Individuals" name="individuals_only" domain="[('is_company', '=', False)]"/>
                
                <separator/>
                <filter string="Active" name="active_members" domain="[('membership_status', '=', 'active')]"/>
                <filter string="Grace Period" name="grace_members" domain="[('membership_status', '=', 'grace')]"/>
                <filter string="Lapsed" name="lapsed_members" domain="[('membership_status', '=', 'lapsed')]"/>
                <filter string="Renewal Due" name="renewal_due" domain="[('is_renewal_due', '=', True)]"/>
                
                <separator/>
                <filter string="Has Email" name="has_email" domain="[('email', '!=', False)]"/>
                <filter string="Has Phone" name="has_phone" domain="['|', ('phone', '!=', False), ('mobile', '!=', False)]"/>
                
                <group expand="0" string="Group By">
                    <filter string="Membership Status" name="group_status" context="{'group_by': 'membership_status'}"/>
                    <filter string="Donor Level" name="group_donor" context="{'group_by': 'donor_level'}"/>
                    <filter string="Gender" name="group_gender" context="{'group_by': 'gender'}"/>
                    <filter string="State" name="group_state" context="{'group_by': 'state_id'}"/>
                    <filter string="Country" name="group_country" context="{'group_by': 'country_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Individual Members Action -->
    <record id="action_partner_individual_members" model="ir.actions.act_window">
        <field name="name">Individual Members</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="domain">[('is_member', '=', True), ('is_company', '=', False)]</field>
        <field name="context">{'default_is_company': False}</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'kanban', 'view_id': ref('view_partner_individual_kanban')}),
            (0, 0, {'view_mode': 'tree', 'view_id': ref('view_partner_individual_tree')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_partner_individual_form')})]"/>
        <field name="search_view_id" ref="view_partner_individual_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first individual member!
            </p>
            <p>
                Individual members are people who have personal memberships with your association.
            </p>
        </field>
    </record>

</odoo>
<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Record Rules -->
        <record model="ir.rule" id="membership_comms_rule_user">
            <field name="name">Membership Communications: User Access</field>
            <field name="model_id" ref="model_membership_comms"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('membership_base.group_membership_user'))]"/>
        </record>

    </data>
</odoo>
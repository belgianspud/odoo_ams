<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Enhanced Chart of Accounts Form -->
    <record id="view_account_account_form_ams" model="ir.ui.view">
        <field name="name">account.account.form.ams</field>
        <field name="model">account.account</field>
        <field name="inherit_id" ref="account.view_account_form"/>
        <field name="arch" type="xml">
            <!-- Add AMS fields after code -->
            <xpath expr="//field[@name='code']" position="after">
                <field name="ams_account_category"/>
                <field name="is_ams_account"/>
            </xpath>
            
            <!-- Add AMS description -->
            <xpath expr="//field[@name='name']" position="after">
                <field name="ams_description" invisible="not ams_account_category"/>
            </xpath>
            
            <!-- Add usage statistics -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_related_products" 
                        type="object" 
                        class="oe_stat_button" 
                        icon="fa-cube"
                        invisible="subscription_count == 0">
                    <field name="subscription_count" widget="statinfo" string="Products"/>
                </button>
            </xpath>
        </field>
    </record>

    <!-- Enhanced Chart of Accounts Tree -->
    <record id="view_account_account_tree_ams" model="ir.ui.view">
        <field name="name">account.account.tree.ams</field>
        <field name="model">account.account</field>
        <field name="inherit_id" ref="account.view_account_list"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='code']" position="after">
                <field name="ams_account_category" optional="hide"/>
                <field name="is_ams_account" string="AMS" optional="show"/>
                <field name="subscription_count" string="Products" optional="hide"/>
            </xpath>
        </field>
    </record>

    <!-- AMS Chart of Accounts Search -->
    <record id="view_account_account_search_ams" model="ir.ui.view">
        <field name="name">account.account.search.ams</field>
        <field name="model">account.account</field>
        <field name="inherit_id" ref="account.view_account_search"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <separator/>
                <filter name="filter_ams_accounts" string="AMS Accounts" 
                        domain="[('is_ams_account', '=', True)]"/>
                <filter name="filter_membership_revenue" string="Membership Revenue" 
                        domain="[('ams_account_category', '=', 'membership_revenue')]"/>
                <filter name="filter_publication_revenue" string="Publication Revenue" 
                        domain="[('ams_account_category', '=', 'publication_revenue')]"/>
                <filter name="filter_deferred_revenue" string="Deferred Revenue" 
                        domain="[('ams_account_category', '=', 'deferred_revenue')]"/>
                <separator/>
                <group expand="0" string="Group By">
                    <filter name="group_by_ams_category" string="AMS Category" 
                            domain="[]" context="{'group_by': 'ams_account_category'}"/>
                </group>
            </xpath>
        </field>
    </record>

    <!-- AMS Chart of Accounts Action -->
    <record id="action_account_account_ams" model="ir.actions.act_window">
        <field name="name">AMS Chart of Accounts</field>
        <field name="res_model">account.account</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('is_ams_account', '=', True)]</field>
        <field name="context">{'search_default_filter_ams_accounts': 1}</field>
    </record>
</odoo>
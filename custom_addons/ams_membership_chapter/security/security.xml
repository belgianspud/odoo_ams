<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!-- Record Rules -->
        <record model="ir.rule" id="membership_chapter_rule_user">
            <field name="name">Membership Chapter: User Access</field>
            <field name="model_id" ref="model_membership_chapter"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('ams_membership_base.group_membership_user'))]"/>
        </record>

        <record model="ir.rule" id="membership_chapter_meeting_rule_user">
            <field name="name">Chapter Meeting: User Access</field>
            <field name="model_id" ref="model_membership_chapter_meeting"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('ams_membership_base.group_membership_user'))]"/>
        </record>

        <record model="ir.rule" id="membership_chapter_budget_rule_user">
            <field name="name">Chapter Budget: User Access</field>
            <field name="model_id" ref="model_membership_chapter_budget"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('ams_membership_base.group_membership_user'))]"/>
        </record>

        <record model="ir.rule" id="membership_chapter_transaction_rule_user">
            <field name="name">Chapter Transaction: User Access</field>
            <field name="model_id" ref="model_membership_chapter_transaction"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('ams_membership_base.group_membership_user'))]"/>
        </record>

    </data>
</odoo>
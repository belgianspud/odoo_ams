<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Enhanced Portal Home Extension -->
        <template id="portal_my_home_membership" name="Portal My Home: Membership" inherit_id="portal.portal_my_home" priority="30">
            <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
                <t t-if="is_member">
                    <div class="row">
                        <!-- Member Status Card -->
                        <div class="col-lg-12 mb-4">
                            <div class="card border-primary">
                                <div class="card-header bg-primary text-white">
                                    <h5 class="mb-0">
                                        <i class="fa fa-id-card me-2"/>Membership Status
                                    </h5>
                                </div>
                                <div class="card-body">
                                    <div class="row">
                                        <div class="col-md-3">
                                            <strong>Member ID:</strong><br/>
                                            <span class="text-primary fs-5" t-esc="member_number"/>
                                        </div>
                                        <div class="col-md-3">
                                            <strong>Member Type:</strong><br/>
                                            <span t-esc="member_type"/>
                                        </div>
                                        <div class="col-md-3">
                                            <strong>Status:</strong><br/>
                                            <span t-attf-class="badge bg-#{member_status == 'active' and 'success' or member_status == 'grace' and 'warning' or 'secondary'}" 
                                                  t-esc="member_status.title()"/>
                                        </div>
                                        <div class="col-md-3">
                                            <strong>Membership Expires:</strong><br/>
                                            <span t-esc="membership_end_date" t-if="membership_end_date"/>
                                            <span t-if="not membership_end_date">Not Set</span>
                                        </div>
                                    </div>
                                    <div class="row mt-3" t-if="next_renewal_date">
                                        <div class="col-12">
                                            <div class="alert alert-info mb-0">
                                                <i class="fa fa-clock-o me-2"/>
                                                <strong>Next Renewal Due:</strong> 
                                                <span t-esc="next_renewal_date"/>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="row mt-3">
                                        <div class="col-12">
                                            <a href="/my/member-profile" class="btn btn-primary btn-sm me-2">
                                                <i class="fa fa-user"/> View Member Profile
                                            </a>
                                            <a href="/my/memberships" class="btn btn-outline-primary btn-sm">
                                                <i class="fa fa-id-card"/> Manage Memberships
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </t>
            </xpath>
            
            <!-- Add membership and subscription counters to portal docs -->
            <xpath expr="//div[contains(@class,'o_portal_docs')]" position="before">
                <t t-if="membership_count" t-call="portal.portal_docs_entry">
                    <t t-set="icon" t-value="'fa-id-card'"/>
                    <t t-set="title">Memberships</t>
                    <t t-set="url" t-value="'/my/memberships'"/>
                    <t t-set="text">Follow your membership status and renewals</t>
                    <t t-set="count" t-value="membership_count"/>
                </t>
                <t t-if="subscription_count" t-call="portal.portal_docs_entry">
                    <t t-set="icon" t-value="'fa-refresh'"/>
                    <t t-set="title">Subscriptions</t>
                    <t t-set="url" t-value="'/my/subscriptions'"/>
                    <t t-set="text">Manage your publication and service subscriptions</t>
                    <t t-set="count" t-value="subscription_count"/>
                </t>
            </xpath>
        </template>

        <!-- Member Profile Dashboard - NEW -->
        <template id="portal_member_profile" name="Member Profile">
            <t t-call="portal.portal_layout">
                <t t-set="page_name" t-value="'member_profile'"/>
                
                <div class="container mt-4">
                    <div class="row">
                        <div class="col-12">
                            <div class="d-flex justify-content-between align-items-center mb-4">
                                <h2><i class="fa fa-user-circle"/> Member Profile</h2>
                                <span t-attf-class="badge bg-#{member_status == 'active' and 'success' or member_status == 'grace' and 'warning' or 'secondary'} fs-6" 
                                      t-esc="member_status.title()"/>
                            </div>
                        </div>
                    </div>
                    
                    <div class="row">
                        <!-- Member Information Card -->
                        <div class="col-lg-6 mb-4">
                            <div class="card h-100">
                                <div class="card-header">
                                    <h5 class="mb-0"><i class="fa fa-id-card me-2"/>Member Information</h5>
                                </div>
                                <div class="card-body">
                                    <table class="table table-borderless">
                                        <tr>
                                            <td width="40%"><strong>Member ID:</strong></td>
                                            <td class="text-primary fs-6"><strong t-esc="member_number"/></td>
                                        </tr>
                                        <tr>
                                            <td><strong>Name:</strong></td>
                                            <td t-esc="partner.name"/>
                                        </tr>
                                        <tr>
                                            <td><strong>Email:</strong></td>
                                            <td t-esc="partner.email"/>
                                        </tr>
                                        <tr t-if="member_type">
                                            <td><strong>Member Type:</strong></td>
                                            <td t-esc="member_type.name"/>
                                        </tr>
                                        <tr>
                                            <td><strong>Status:</strong></td>
                                            <td>
                                                <span t-attf-class="badge bg-#{member_status == 'active' and 'success' or member_status == 'grace' and 'warning' or 'secondary'}" 
                                                      t-esc="member_status.title()"/>
                                            </td>
                                        </tr>
                                        <tr t-if="membership_start_date">
                                            <td><strong>Member Since:</strong></td>
                                            <td t-esc="membership_start_date"/>
                                        </tr>
                                    </table>
                                </div>
                            </div>
                        </div>

                        <!-- Current Membership Card -->
                        <div class="col-lg-6 mb-4">
                            <div class="card h-100">
                                <div class="card-header">
                                    <h5 class="mb-0"><i class="fa fa-certificate me-2"/>Current Membership</h5>
                                </div>
                                <div class="card-body">
                                    <t t-if="current_membership">
                                        <table class="table table-borderless">
                                            <tr>
                                                <td width="40%"><strong>Product:</strong></td>
                                                <td t-esc="current_membership.product_id.name"/>
                                            </tr>
                                            <tr>
                                                <td><strong>Start Date:</strong></td>
                                                <td t-field="current_membership.start_date"/>
                                            </tr>
                                            <tr>
                                                <td><strong>End Date:</strong></td>
                                                <td t-field="current_membership.end_date"/>
                                            </tr>
                                            <tr>
                                                <td><strong>Auto Renew:</strong></td>
                                                <td>
                                                    <span t-if="current_membership.auto_renew" class="text-success">
                                                        <i class="fa fa-check"/> Enabled
                                                    </span>
                                                    <span t-else="" class="text-muted">
                                                        <i class="fa fa-times"/> Disabled
                                                    </span>
                                                </td>
                                            </tr>
                                            <tr>
                                                <td><strong>Payment Status:</strong></td>
                                                <td>
                                                    <span t-attf-class="badge bg-#{current_membership.payment_status == 'paid' and 'success' or current_membership.payment_status == 'pending' and 'warning' or 'secondary'}"
                                                          t-esc="current_membership.payment_status.title()"/>
                                                </td>
                                            </tr>
                                        </table>
                                        <div class="text-end mt-3">
                                            <a t-attf-href="/my/memberships/#{current_membership.id}" class="btn btn-primary btn-sm">
                                                <i class="fa fa-eye"/> View Details
                                            </a>
                                        </div>
                                    </t>
                                    <t t-else="">
                                        <div class="alert alert-info">
                                            <h6>No Active Membership</h6>
                                            <p class="mb-0">You don't currently have an active membership.</p>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </div>

                        <!-- Active Benefits -->
                        <div class="col-12 mb-4" t-if="active_benefits">
                            <div class="card">
                                <div class="card-header">
                                    <h5 class="mb-0"><i class="fa fa-gift me-2"/>Active Benefits</h5>
                                </div>
                                <div class="card-body">
                                    <div class="row">
                                        <t t-foreach="active_benefits" t-as="benefit">
                                            <div class="col-md-6 col-lg-4 mb-3">
                                                <div class="card border-success border-opacity-25 h-100">
                                                    <div class="card-body text-center">
                                                        <h6 class="card-title">
                                                            <i class="fa fa-check-circle text-success me-2"/>
                                                            <span t-esc="benefit.name"/>
                                                        </h6>
                                                        <small class="text-muted" t-esc="benefit.benefit_type.title()"/>
                                                        <t t-if="benefit.benefit_type == 'discount' and benefit.discount_percentage">
                                                            <div class="badge bg-success mt-2">
                                                                <t t-esc="benefit.discount_percentage"/>% Discount
                                                            </div>
                                                        </t>
                                                    </div>
                                                </div>
                                            </div>
                                        </t>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Quick Actions -->
                        <div class="col-12">
                            <div class="card">
                                <div class="card-header">
                                    <h5 class="mb-0"><i class="fa fa-bolt me-2"/>Quick Actions</h5>
                                </div>
                                <div class="card-body">
                                    <div class="d-flex gap-2 flex-wrap">
                                        <a href="/my/memberships" class="btn btn-outline-primary">
                                            <i class="fa fa-id-card"/> View All Memberships
                                        </a>
                                        <a href="/my/subscriptions" class="btn btn-outline-info" t-if="subscription_count">
                                            <i class="fa fa-refresh"/> View Subscriptions
                                        </a>
                                        <t t-if="current_membership and not current_membership.auto_renew">
                                            <a t-attf-href="/my/memberships/#{current_membership.id}/renew" class="btn btn-success">
                                                <i class="fa fa-refresh"/> Renew Membership
                                            </a>
                                        </t>
                                        <t t-if="pending_renewals_count">
                                            <a href="/my/renewals" class="btn btn-warning">
                                                <i class="fa fa-clock-o"/> Pending Renewals (<t t-esc="pending_renewals_count"/>)
                                            </a>
                                        </t>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </t>
        </template>

        <!-- My Memberships List - Enhanced -->
        <template id="portal_my_memberships" name="My Memberships">
            <t t-call="portal.portal_layout">
                <t t-set="page_name" t-value="'membership'"/>
                
                <div class="container mt-4">
                    <div class="row">
                        <div class="col-12">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <h2><i class="fa fa-id-card"/> My Memberships</h2>
                                <t t-if="is_member">
                                    <a href="/my/member-profile" class="btn btn-outline-primary">
                                        <i class="fa fa-user"/> Member Profile
                                    </a>
                                </t>
                            </div>

                            <!-- Member Summary -->
                            <t t-if="is_member">
                                <div class="alert alert-primary mb-4">
                                    <div class="row">
                                        <div class="col-md-3">
                                            <strong>Member ID:</strong> <span class="text-primary" t-esc="member_number"/>
                                        </div>
                                        <div class="col-md-3" t-if="member_type">
                                            <strong>Type:</strong> <span t-esc="member_type.name"/>
                                        </div>
                                        <div class="col-md-3">
                                            <strong>Status:</strong> 
                                            <span t-attf-class="badge bg-#{member_status == 'active' and 'success' or member_status == 'grace' and 'warning' or 'secondary'}" 
                                                  t-esc="member_status.title()"/>
                                        </div>
                                        <div class="col-md-3" t-if="membership_end_date">
                                            <strong>Expires:</strong> <span t-field="membership_end_date"/>
                                        </div>
                                    </div>
                                </div>
                            </t>
                            
                            <t t-if="not memberships">
                                <div class="alert alert-info">
                                    <h4>No memberships found</h4>
                                    <p>You don't have any memberships yet. Contact us to learn about membership options.</p>
                                    <a href="/contactus" class="btn btn-primary">Contact Us</a>
                                </div>
                            </t>
                            
                            <t t-if="memberships">
                                <div class="row">
                                    <t t-foreach="memberships" t-as="membership">
                                        <div class="col-lg-6 col-12 mb-4">
                                            <div class="card h-100">
                                                <div class="card-header d-flex justify-content-between align-items-center">
                                                    <h6 class="mb-0" t-esc="membership.product_id.name"/>
                                                    <span t-attf-class="badge bg-#{membership.state == 'active' and 'success' or membership.state == 'grace' and 'warning' or 'secondary'}" t-esc="membership.state.title()"/>
                                                </div>
                                                <div class="card-body">
                                                    <p class="text-muted mb-2">
                                                        Membership ID: <span t-esc="membership.name"/>
                                                    </p>
                                                    <div class="row text-sm">
                                                        <div class="col-6">
                                                            <strong>Start:</strong><br/>
                                                            <span t-field="membership.start_date"/>
                                                        </div>
                                                        <div class="col-6">
                                                            <strong>End:</strong><br/>
                                                            <span t-field="membership.end_date"/>
                                                        </div>
                                                    </div>
                                                    <t t-if="membership.days_until_expiry and membership.days_until_expiry &lt; 30 and membership.days_until_expiry &gt; 0">
                                                        <div class="alert alert-warning mt-2 py-1">
                                                            <i class="fa fa-clock-o"/> Expires in <strong t-esc="membership.days_until_expiry"/> days
                                                        </div>
                                                    </t>
                                                    <div class="mt-2">
                                                        <small class="text-muted">
                                                            Payment: 
                                                            <span t-attf-class="badge bg-#{membership.payment_status == 'paid' and 'success' or membership.payment_status == 'pending' and 'warning' or 'secondary'} badge-sm" 
                                                                  t-esc="membership.payment_status.title()"/>
                                                        </small>
                                                    </div>
                                                </div>
                                                <div class="card-footer">
                                                    <div class="d-flex gap-2">
                                                        <a t-attf-href="/my/memberships/#{membership.id}" class="btn btn-sm btn-outline-primary">
                                                            <i class="fa fa-eye"/> View Details
                                                        </a>
                                                        <t t-if="membership.state == 'active' and not membership.auto_renew">
                                                            <a t-attf-href="/my/memberships/#{membership.id}/renew" class="btn btn-sm btn-success">
                                                                <i class="fa fa-refresh"/> Renew
                                                            </a>
                                                        </t>
                                                        <t t-if="membership.invoice_id">
                                                            <a t-attf-href="/my/invoices/#{membership.invoice_id.id}" class="btn btn-sm btn-outline-secondary">
                                                                <i class="fa fa-file-text-o"/> Invoice
                                                            </a>
                                                        </t>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                            </t>
                        </div>
                    </div>
                </div>
            </t>
        </template>

        <!-- Enhanced Membership Detail Template -->
        <template id="portal_membership_detail" name="Membership Details">
            <t t-call="portal.portal_layout">
                <t t-set="page_name" t-value="'membership'"/>
                
                <div class="container mt-4">
                    <div class="row justify-content-center">
                        <div class="col-lg-8">
                            <nav aria-label="breadcrumb">
                                <ol class="breadcrumb">
                                    <li class="breadcrumb-item">
                                        <a href="/my">My Account</a>
                                    </li>
                                    <li class="breadcrumb-item">
                                        <a href="/my/memberships">Memberships</a>
                                    </li>
                                    <li class="breadcrumb-item active" t-esc="membership.product_id.name"/>
                                </ol>
                            </nav>

                            <div class="card">
                                <div class="card-header">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <h3 class="mb-0">
                                            <i class="fa fa-id-card"/> <span t-esc="membership.product_id.name"/>
                                        </h3>
                                        <span t-attf-class="badge bg-#{membership.state == 'active' and 'success' or membership.state == 'grace' and 'warning' or 'secondary'} fs-6" t-esc="membership.state.title()"/>
                                    </div>
                                    <div class="text-muted mt-2">
                                        <strong>Member ID:</strong> <span class="text-primary" t-esc="member_number"/> | 
                                        <strong>Membership ID:</strong> <span t-esc="membership.name"/>
                                    </div>
                                </div>
                                
                                <div class="card-body">
                                    <div class="row mb-4">
                                        <div class="col-md-6">
                                            <h5>Membership Period</h5>
                                            <table class="table table-borderless table-sm">
                                                <tr>
                                                    <td width="40%"><strong>Start Date:</strong></td>
                                                    <td t-field="membership.start_date"/>
                                                </tr>
                                                <tr>
                                                    <td><strong>End Date:</strong></td>
                                                    <td t-field="membership.end_date"/>
                                                </tr>
                                                <tr t-if="membership.last_renewal_date">
                                                    <td><strong>Last Renewed:</strong></td>
                                                    <td t-field="membership.last_renewal_date"/>
                                                </tr>
                                                <tr t-if="membership.days_until_expiry and membership.days_until_expiry > 0">
                                                    <td><strong>Days Remaining:</strong></td>
                                                    <td>
                                                        <span t-attf-class="badge bg-#{membership.days_until_expiry &lt; 30 and 'warning' or 'info'}"
                                                              t-esc="membership.days_until_expiry"/>
                                                    </td>
                                                </tr>
                                            </table>
                                        </div>
                                        
                                        <div class="col-md-6">
                                            <h5>Details</h5>
                                            <table class="table table-borderless table-sm">
                                                <tr t-if="membership.member_type_id">
                                                    <td width="40%"><strong>Member Type:</strong></td>
                                                    <td t-field="membership.member_type_id.name"/>
                                                </tr>
                                                <tr>
                                                    <td><strong>Auto Renew:</strong></td>
                                                    <td>
                                                        <span t-if="membership.auto_renew" class="text-success">
                                                            <i class="fa fa-check"/> Enabled
                                                        </span>
                                                        <span t-else="" class="text-muted">
                                                            <i class="fa fa-times"/> Disabled
                                                        </span>
                                                    </td>
                                                </tr>
                                                <tr>
                                                    <td><strong>Payment:</strong></td>
                                                    <td>
                                                        <span t-attf-class="badge bg-#{membership.payment_status == 'paid' and 'success' or membership.payment_status == 'pending' and 'warning' or 'secondary'}" t-esc="membership.payment_status.title()"/>
                                                    </td>
                                                </tr>
                                                <tr t-if="membership.membership_fee">
                                                    <td><strong>Membership Fee:</strong></td>
                                                    <td>
                                                        <span t-field="membership.membership_fee" t-options="{'widget': 'monetary'}"/>
                                                    </td>
                                                </tr>
                                            </table>
                                        </div>
                                    </div>
                                    
                                    <!-- Benefits Section -->
                                    <t t-if="membership.benefit_ids">
                                        <h5>Your Benefits</h5>
                                        <div class="row mb-4">
                                            <t t-foreach="membership.benefit_ids" t-as="benefit">
                                                <div class="col-md-6 mb-3">
                                                    <div class="card border-primary border-opacity-25">
                                                        <div class="card-body">
                                                            <h6 class="card-title">
                                                                <i class="fa fa-gift text-primary"/> <span t-esc="benefit.name"/>
                                                            </h6>
                                                            <p class="card-text text-muted" t-field="benefit.description"/>
                                                            <t t-if="benefit.benefit_type == 'discount' and benefit.discount_percentage">
                                                                <span class="badge bg-primary">
                                                                    <t t-esc="benefit.discount_percentage"/>% Discount
                                                                </span>
                                                            </t>
                                                        </div>
                                                    </div>
                                                </div>
                                            </t>
                                        </div>
                                    </t>
                                    
                                    <!-- Action Buttons -->
                                    <div class="d-flex gap-2 justify-content-end">
                                        <t t-if="membership.state == 'active' and not membership.auto_renew">
                                            <a t-attf-href="/my/memberships/#{membership.id}/renew" class="btn btn-success">
                                                <i class="fa fa-refresh"/> Renew Membership
                                            </a>
                                        </t>
                                        
                                        <t t-if="membership.invoice_id">
                                            <a t-attf-href="/my/invoices/#{membership.invoice_id.id}" class="btn btn-outline-primary">
                                                <i class="fa fa-file-text-o"/> View Invoice
                                            </a>
                                        </t>
                                        
                                        <a href="/my/memberships" class="btn btn-outline-secondary">
                                            <i class="fa fa-arrow-left"/> Back to Memberships
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </t>
        </template>

        <!-- My Subscriptions List - Enhanced -->
        <template id="portal_my_subscriptions" name="My Subscriptions">
            <t t-call="portal.portal_layout">
                <t t-set="page_name" t-value="'subscription'"/>
                
                <div class="container mt-4">
                    <div class="row">
                        <div class="col-12">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <h2><i class="fa fa-refresh"/> My Subscriptions</h2>
                            </div>
                            
                            <t t-if="not subscriptions">
                                <div class="alert alert-info">
                                    <h4>No subscriptions found</h4>
                                    <p>You don't have any subscriptions yet. Browse our publications and services to get started.</p>
                                </div>
                            </t>
                            
                            <t t-if="subscriptions">
                                <div class="row">
                                    <t t-foreach="subscriptions" t-as="subscription">
                                        <div class="col-lg-6 col-12 mb-4">
                                            <div class="card h-100">
                                                <div class="card-header d-flex justify-content-between align-items-center">
                                                    <h6 class="mb-0" t-esc="subscription.product_id.name"/>
                                                    <div>
                                                        <span class="badge bg-info me-1" t-esc="subscription.subscription_type.title()"/>
                                                        <span t-attf-class="badge bg-#{subscription.state == 'active' and 'success' or subscription.state == 'paused' and 'info' or subscription.state == 'suspended' and 'warning' or 'secondary'}" t-esc="subscription.state.title()"/>
                                                    </div>
                                                </div>
                                                <div class="card-body">
                                                    <p class="text-muted mb-2">
                                                        ID: <span t-esc="subscription.name"/>
                                                    </p>
                                                    <div class="row text-sm">
                                                        <div class="col-6">
                                                            <strong>Start:</strong><br/>
                                                            <span t-field="subscription.start_date"/>
                                                        </div>
                                                        <div class="col-6">
                                                            <strong>End:</strong><br/>
                                                            <span t-field="subscription.end_date"/>
                                                        </div>
                                                    </div>
                                                    
                                                    <!-- Publication specific info -->
                                                    <t t-if="subscription.subscription_type == 'publication'">
                                                        <div class="mt-2">
                                                            <span t-if="subscription.digital_access" class="badge bg-light text-dark me-1">
                                                                <i class="fa fa-desktop"/> Digital
                                                            </span>
                                                            <span t-if="subscription.print_delivery" class="badge bg-light text-dark">
                                                                <i class="fa fa-print"/> Print
                                                            </span>
                                                        </div>
                                                    </t>
                                                </div>
                                                <div class="card-footer">
                                                    <a t-attf-href="/my/subscriptions/#{subscription.id}" class="btn btn-sm btn-outline-primary">
                                                        <i class="fa fa-eye"/> View Details
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                    </t>
                                </div>
                            </t>
                        </div>
                    </div>
                </div>
            </t>
        </template>

        <!-- Subscription Detail - Enhanced -->
        <template id="portal_subscription_detail" name="Subscription Details">
            <t t-call="portal.portal_layout">
                <t t-set="page_name" t-value="'subscription'"/>
                
                <div class="container mt-4">
                    <div class="row justify-content-center">
                        <div class="col-lg-8">
                            <nav aria-label="breadcrumb">
                                <ol class="breadcrumb">
                                    <li class="breadcrumb-item">
                                        <a href="/my">My Account</a>
                                    </li>
                                    <li class="breadcrumb-item">
                                        <a href="/my/subscriptions">Subscriptions</a>
                                    </li>
                                    <li class="breadcrumb-item active" t-esc="subscription.product_id.name"/>
                                </ol>
                            </nav>

                            <div class="card">
                                <div class="card-header">
                                    <div class="d-flex justify-content-between align-items-center">
                                        <h3 class="mb-0">
                                            <i class="fa fa-refresh"/> <span t-esc="subscription.product_id.name"/>
                                        </h3>
                                        <div>
                                            <span class="badge bg-info me-1" t-esc="subscription.subscription_type.title()"/>
                                            <span t-attf-class="badge bg-#{subscription.state == 'active' and 'success' or subscription.state == 'paused' and 'info' or subscription.state == 'suspended' and 'warning' or 'secondary'} fs-6" t-esc="subscription.state.title()"/>
                                        </div>
                                    </div>
                                    <span class="text-muted">Subscription ID: <span t-esc="subscription.name"/></span>
                                </div>
                                
                                <div class="card-body">
                                    <div class="row mb-4">
                                        <div class="col-md-6">
                                            <h5>Subscription Period</h5>
                                            <table class="table table-borderless table-sm">
                                                <tr>
                                                    <td width="40%"><strong>Start Date:</strong></td>
                                                    <td t-field="subscription.start_date"/>
                                                </tr>
                                                <tr>
                                                    <td><strong>End Date:</strong></td>
                                                    <td t-field="subscription.end_date"/>
                                                </tr>
                                                <tr t-if="subscription.last_renewal_date">
                                                    <td><strong>Last Renewed:</strong></td>
                                                    <td t-field="subscription.last_renewal_date"/>
                                                </tr>
                                            </table>
                                        </div>
                                        
                                        <div class="col-md-6">
                                            <h5>Details</h5>
                                            <table class="table table-borderless table-sm">
                                                <tr>
                                                    <td width="40%"><strong>Type:</strong></td>
                                                    <td t-esc="subscription.subscription_type.title()"/>
                                                </tr>
                                                <tr>
                                                    <td><strong>Auto Renew:</strong></td>
                                                    <td>
                                                        <span t-if="subscription.auto_renew" class="text-success">
                                                            <i class="fa fa-check"/> Enabled
                                                        </span>
                                                        <span t-else="" class="text-muted">
                                                            <i class="fa fa-times"/> Disabled
                                                        </span>
                                                    </td>
                                                </tr>
                                                
                                                <!-- Publication-specific details -->
                                                <t t-if="subscription.subscription_type == 'publication'">
                                                    <tr>
                                                        <td><strong>Digital Access:</strong></td>
                                                        <td>
                                                            <span t-if="subscription.digital_access" class="text-success">
                                                                <i class="fa fa-check"/> Yes
                                                            </span>
                                                            <span t-else="" class="text-muted">
                                                                <i class="fa fa-times"/> No
                                                            </span>
                                                        </td>
                                                    </tr>
                                                    <tr>
                                                        <td><strong>Print Delivery:</strong></td>
                                                        <td>
                                                            <span t-if="subscription.print_delivery" class="text-success">
                                                                <i class="fa fa-check"/> Yes
                                                            </span>
                                                            <span t-else="" class="text-muted">
                                                                <i class="fa fa-times"/> No
                                                            </span>
                                                        </td>
                                                    </tr>
                                                </t>
                                            </table>
                                        </div>
                                    </div>
                                    
                                    <!-- Benefits Section -->
                                    <t t-if="subscription.benefit_ids">
                                        <h5>Subscription Benefits</h5>
                                        <div class="row mb-4">
                                            <t t-foreach="subscription.benefit_ids" t-as="benefit">
                                                <div class="col-md-6 mb-3">
                                                    <div class="card border-info border-opacity-25">
                                                        <div class="card-body">
                                                            <h6 class="card-title">
                                                                <i class="fa fa-gift text-info"/> <span t-esc="benefit.name"/>
                                                            </h6>
                                                            <p class="card-text text-muted" t-field="benefit.description"/>
                                                        </div>
                                                    </div>
                                                </div>
                                            </t>
                                        </div>
                                    </t>
                                    
                                    <!-- Action Buttons -->
                                    <div class="d-flex gap-2 justify-content-end">
                                        <t t-if="subscription.state == 'active'">
                                            <form method="post" t-attf-action="/my/subscriptions/#{subscription.id}/pause" style="display:inline;">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <button type="submit" class="btn btn-warning btn-sm">
                                                    <i class="fa fa-pause"/> Pause
                                                </button>
                                            </form>
                                        </t>
                                        
                                        <t t-if="subscription.state == 'paused'">
                                            <form method="post" t-attf-action="/my/subscriptions/#{subscription.id}/resume" style="display:inline;">
                                                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                                <button type="submit" class="btn btn-success btn-sm">
                                                    <i class="fa fa-play"/> Resume
                                                </button>
                                            </form>
                                        </t>
                                        
                                        <a href="/my/subscriptions" class="btn btn-outline-secondary">
                                            <i class="fa fa-arrow-left"/> Back to Subscriptions
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </t>
        </template>

        <!-- No Membership Access Template -->
        <template id="portal_no_membership_access" name="No Membership Access">
            <t t-call="portal.portal_layout">
                <div class="container mt-4">
                    <div class="row justify-content-center">
                        <div class="col-lg-8">
                            <div class="alert alert-info">
                                <h4>Membership Portal</h4>
                                <p>This section is available for members only.</p>
                                <a href="/contactus" class="btn btn-primary">Contact Us About Membership</a>
                            </div>
                        </div>
                    </div>
                </div>
            </t>
        </template>

    </data>
</odoo>
<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- AMS Membership List View - Enhanced with Chapter Support -->
        <record id="view_ams_membership_list" model="ir.ui.view">
            <field name="name">ams.membership.list</field>
            <field name="model">ams.membership</field>
            <field name="arch" type="xml">
                <list string="Memberships" default_order="start_date desc" 
                      decoration-info="is_chapter_membership" 
                      decoration-success="state=='active'" 
                      decoration-warning="state=='grace'"
                      decoration-danger="state=='terminated'">
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="member_type_id" optional="show"/>
                    <field name="product_id"/>
                    <field name="membership_type" optional="show"/>
                    <field name="chapter_type" optional="hide" invisible="not is_chapter_membership"/>
                    <field name="chapter_location" optional="hide" invisible="not is_chapter_membership"/>
                    <field name="chapter_role" optional="hide" invisible="not is_chapter_membership"/>
                    <field name="chapter_engagement_score" optional="hide" invisible="not is_chapter_membership" widget="progressbar"/>
                    <field name="start_date"/>
                    <field name="end_date"/>
                    <field name="state"/>
                    <field name="membership_fee" widget="monetary" optional="show"/>
                    <field name="payment_status" optional="hide"/>
                    <field name="auto_renew" optional="hide"/>
                    <field name="renewal_interval" optional="hide"/>
                    <field name="days_until_expiry" optional="hide"/>
                    <field name="is_chapter_membership" invisible="1"/>
                </list>
            </field>
        </record>

        <!-- AMS Membership Form View - Enhanced Chapter Support -->
        <record id="view_ams_membership_form" model="ir.ui.view">
            <field name="name">ams.membership.form</field>
            <field name="model">ams.membership</field>
            <field name="arch" type="xml">
                <form string="Membership">
                    <header>
                        <button name="action_activate" type="object" string="Activate" 
                                class="oe_highlight" invisible="state != 'draft'"
                                groups="ams_membership_core.group_membership_user"/>
                        <button name="action_renew" type="object" string="Renew" 
                                class="btn-primary" invisible="state not in ['active', 'grace']"
                                groups="ams_membership_core.group_membership_user"/>
                        
                        <!-- Chapter-specific action buttons -->
                        <button name="action_record_chapter_activity" type="object" string="Record Activity"
                                class="btn-info" invisible="not is_chapter_membership or state != 'active'"
                                groups="ams_membership_core.group_membership_user"/>
                        <button name="action_transfer_chapter" type="object" string="Transfer Chapter"
                                class="btn-warning" invisible="not is_chapter_membership or state != 'active'"
                                groups="ams_membership_core.group_membership_manager"/>
                        <button name="action_chapter_dashboard" type="object" string="Chapter Dashboard"
                                class="btn-secondary" invisible="not is_chapter_membership"
                                groups="ams_membership_core.group_membership_user"/>
                        
                        <button name="action_suspend" type="object" string="Suspend" 
                                invisible="state not in ['active', 'grace']"
                                groups="ams_membership_core.group_membership_manager"
                                confirm="Are you sure you want to suspend this membership?"/>
                        <button name="action_terminate" type="object" string="Terminate" 
                                invisible="state == 'terminated'"
                                groups="ams_membership_core.group_membership_manager"
                                confirm="Are you sure you want to terminate this membership?"/>
                        <field name="state" widget="statusbar" 
                               statusbar_visible="draft,active,grace,suspended,terminated"/>
                    </header>
                    
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button name="action_view_invoice" type="object" 
                                    class="oe_stat_button" icon="fa-file-text-o"
                                    invisible="not invoice_id"
                                    groups="account.group_account_invoice">
                                <div class="o_stat_info">
                                    <span class="o_stat_text">View</span>
                                    <span class="o_stat_text">Invoice</span>
                                </div>
                            </button>
                            
                            <!-- Chapter-specific stat buttons -->
                            <button type="object" class="oe_stat_button" icon="fa-map-marker"
                                    invisible="not is_chapter_membership">
                                <div class="o_stat_info">
                                    <span class="o_stat_text" t-esc="chapter_type and chapter_type.replace('_', ' ').title() or 'Chapter'"/>
                                    <span class="o_stat_text">Type</span>
                                </div>
                            </button>
                            
                            <button type="object" class="oe_stat_button" icon="fa-key"
                                    invisible="not is_chapter_membership">
                                <div class="o_stat_info">
                                    <span class="o_stat_text" t-esc="chapter_access_level and chapter_access_level.replace('_', ' ').title() or 'Basic'"/>
                                    <span class="o_stat_text">Access</span>
                                </div>
                            </button>
                            
                            <button type="object" class="oe_stat_button" icon="fa-trophy"
                                    invisible="not is_chapter_membership">
                                <div class="o_stat_info">
                                    <field name="chapter_engagement_score" widget="statinfo" string="Engagement"/>
                                </div>
                            </button>
                            
                            <button type="object" class="oe_stat_button" icon="fa-calendar"
                                    invisible="not is_chapter_membership or not chapter_events_attended">
                                <div class="o_stat_info">
                                    <field name="chapter_events_attended" widget="statinfo" string="Events"/>
                                </div>
                            </button>
                            
                            <button type="object" class="oe_stat_button" icon="fa-clock-o"
                                    invisible="not is_chapter_membership or not chapter_volunteer_hours">
                                <div class="o_stat_info">
                                    <field name="chapter_volunteer_hours" widget="statinfo" string="Vol. Hours"/>
                                </div>
                            </button>
                        </div>

                        <div class="oe_title">
                            <h1>
                                <field name="display_name" readonly="True"/>
                            </h1>
                            <div class="o_row">
                                <field name="name" readonly="True"/>
                                <span t-if="is_chapter_membership" class="badge bg-info ms-2">
                                    <i class="fa fa-map-marker"/> Chapter Membership
                                </span>
                            </div>
                        </div>

                        <group>
                            <group string="Member Information">
                                <field name="partner_id" required="True" 
                                       domain="[('is_member', '=', True)]"/>
                                <field name="member_type_id" readonly="True"/>
                                <field name="product_id" required="True"
                                       domain="[('is_subscription_product', '=', True), 
                                               ('subscription_product_type', 'in', ['membership', 'chapter'])]"/>
                                <field name="membership_type" readonly="True"/>
                                <field name="is_chapter_membership" invisible="1"/>
                            </group>
                            <group string="Membership Period">
                                <field name="start_date" required="True"/>
                                <field name="end_date" required="True"/>
                                <field name="last_renewal_date"/>
                                <field name="membership_duration" readonly="True" widget="integer"/>
                            </group>
                        </group>

                        <!-- Enhanced Chapter Information Section -->
                        <group string="Chapter Information" invisible="not is_chapter_membership">
                            <group string="Chapter Details">
                                <field name="chapter_type" readonly="True"/>
                                <field name="chapter_location" readonly="True"/>
                                <field name="chapter_city" readonly="True" invisible="not chapter_city"/>
                                <field name="chapter_state" readonly="True" invisible="not chapter_state"/>
                                <field name="chapter_country_id" readonly="True" invisible="not chapter_country_id"/>
                                <field name="chapter_access_level" readonly="True"/>
                            </group>
                            <group string="Chapter Role &amp; Participation">
                                <field name="chapter_role"/>
                                <field name="chapter_position_start_date" invisible="chapter_role == 'member'"/>
                                <field name="chapter_position_end_date" invisible="chapter_role == 'member'"/>
                                <field name="chapter_committee_ids" widget="many2many_tags" 
                                       invisible="not chapter_committee_ids"/>
                            </group>
                        </group>

                        <!-- Enhanced Chapter Access Section -->
                        <group string="Chapter Access &amp; Benefits" invisible="not is_chapter_membership">
                            <group string="Access Permissions">
                                <field name="has_local_events_access" readonly="True"/>
                                <field name="has_chapter_documents_access" readonly="True"/>
                                <field name="has_chapter_training_access" readonly="True"/>
                                <field name="has_networking_access" readonly="True"/>
                                <field name="has_mentorship_access" readonly="True"/>
                                <field name="has_certification_access" readonly="True"/>
                            </group>
                            <group string="Additional Benefits">
                                <field name="has_job_board_access" readonly="True"/>
                                <field name="has_newsletter_access" readonly="True"/>
                            </group>
                        </group>

                        <!-- Chapter Participation Metrics -->
                        <group string="Chapter Participation" invisible="not is_chapter_membership">
                            <group string="Activity Metrics">
                                <field name="chapter_events_attended" readonly="True"/>
                                <field name="chapter_volunteer_hours" readonly="True"/>
                                <field name="chapter_meeting_attendance" readonly="True" widget="percentage"/>
                                <field name="chapter_committee_participation" readonly="True"/>
                                <field name="chapter_last_activity_date" readonly="True"/>
                            </group>
                            <group string="Engagement">
                                <field name="chapter_engagement_score" readonly="True" widget="progressbar"/>
                                <label for="chapter_engagement_score" string="Engagement Score"/>
                                <div>
                                    <field name="chapter_engagement_score" nolabel="1" readonly="True"/>
                                    <span>/100 points</span>
                                </div>
                            </group>
                        </group>

                        <!-- Chapter Transfer Information -->
                        <group string="Chapter Transfer History" invisible="not is_chapter_membership or not previous_chapter_id">
                            <group>
                                <field name="previous_chapter_id" readonly="True"/>
                                <field name="transfer_date" readonly="True"/>
                            </group>
                            <group>
                                <field name="transfer_reason" readonly="True"/>
                            </group>
                        </group>

                        <!-- Geographic Information for Chapters -->
                        <group string="Geographic Information" invisible="not is_chapter_membership">
                            <group>
                                <field name="member_zip_code" readonly="True"/>
                                <field name="member_state" readonly="True"/>
                                <field name="member_country_id" readonly="True"/>
                            </group>
                            <group>
                                <field name="chapter_geographic_restriction" readonly="True"/>
                                <field name="geographic_distance_km" readonly="True" 
                                       invisible="not chapter_geographic_restriction"/>
                            </group>
                        </group>

                        <group>
                            <group string="Renewal Settings">
                                <field name="auto_renew"/>
                                <field name="renewal_interval"/>
                                <field name="next_renewal_date" readonly="True"/>
                            </group>
                            <group string="Financial">
                                <field name="membership_fee" widget="monetary"/>
                                <field name="currency_id" invisible="True"/>
                                <field name="payment_status"/>
                            </group>
                        </group>

                        <group>
                            <group string="Status Information">
                                <field name="is_expired" readonly="True"/>
                                <field name="days_until_expiry" readonly="True"/>
                                <field name="grace_end_date" readonly="True" 
                                       invisible="state != 'grace'"/>
                                <field name="suspension_end_date" readonly="True" 
                                       invisible="state != 'suspended'"/>
                            </group>
                            <group string="Portal &amp; Access">
                                <field name="has_portal_access" readonly="True"/>
                            </group>
                        </group>

                        <notebook>
                            <page string="Benefits" name="benefits">
                                <field name="benefit_ids" nolabel="1">
                                    <list editable="bottom">
                                        <field name="name"/>
                                        <field name="code"/>
                                        <field name="benefit_type"/>
                                        <field name="active"/>
                                    </list>
                                </field>
                            </page>

                            <!-- Enhanced Chapter Access Summary Page -->
                            <page string="Chapter Dashboard" name="chapter_dashboard" 
                                  invisible="not is_chapter_membership">
                                <div class="row">
                                    <div class="col-12">
                                        <div class="alert alert-info">
                                            <h5><i class="fa fa-info-circle"/> Chapter Access Summary</h5>
                                            <p class="mb-1">
                                                <strong>Access Level:</strong> <span t-esc="chapter_access_level and chapter_access_level.replace('_', ' ').title() or 'Basic'"/>
                                            </p>
                                            <p class="mb-1">
                                                <strong>Role:</strong> <span t-esc="chapter_role and chapter_role.replace('_', ' ').title() or 'Member'"/>
                                            </p>
                                            <p class="mb-0">
                                                <strong>Engagement Score:</strong> <span t-esc="chapter_engagement_score or 0"/>/100
                                            </p>
                                        </div>
                                    </div>
                                </div>
                                
                                <group>
                                    <group string="Access Permissions">
                                        <field name="has_local_events_access" readonly="True"/>
                                        <field name="has_chapter_documents_access" readonly="True"/>
                                        <field name="has_chapter_training_access" readonly="True"/>
                                        <field name="has_networking_access" readonly="True"/>
                                        <field name="has_mentorship_access" readonly="True"/>
                                        <field name="has_certification_access" readonly="True"/>
                                        <field name="has_job_board_access" readonly="True"/>
                                        <field name="has_newsletter_access" readonly="True"/>
                                    </group>
                                    <group string="Chapter Information">
                                        <field name="chapter_type" readonly="True"/>
                                        <field name="chapter_location" readonly="True"/>
                                        <field name="chapter_city" readonly="True"/>
                                        <field name="chapter_state" readonly="True"/>
                                        <field name="chapter_country_id" readonly="True"/>
                                    </group>
                                </group>

                                <separator string="Participation Summary"/>
                                <group>
                                    <group string="Activity Statistics">
                                        <field name="chapter_events_attended" readonly="True"/>
                                        <field name="chapter_volunteer_hours" readonly="True"/>
                                        <field name="chapter_meeting_attendance" readonly="True" widget="percentage"/>
                                        <field name="chapter_committee_participation" readonly="True"/>
                                    </group>
                                    <group string="Engagement Details">
                                        <field name="chapter_last_activity_date" readonly="True"/>
                                        <field name="chapter_engagement_score" readonly="True" widget="progressbar"/>
                                    </group>
                                </group>
                            </page>

                            <!-- Chapter Activities Page -->
                            <page string="Chapter Activities" name="chapter_activities" 
                                  invisible="not is_chapter_membership">
                                <field name="chapter_activity_ids" nolabel="1">
                                    <list string="Chapter Activities" default_order="activity_date desc">
                                        <field name="activity_type"/>
                                        <field name="activity_date"/>
                                        <field name="description"/>
                                        <field name="attended"/>
                                        <field name="hours"/>
                                        <field name="notes"/>
                                    </list>
                                    <form string="Chapter Activity">
                                        <group>
                                            <group>
                                                <field name="activity_type"/>
                                                <field name="activity_date"/>
                                                <field name="attended"/>
                                                <field name="hours"/>
                                            </group>
                                            <group>
                                                <field name="description"/>
                                                <field name="notes"/>
                                            </group>
                                        </group>
                                    </form>
                                </field>
                            </page>
                            
                            <page string="Renewals" name="renewals">
                                <field name="renewal_ids" nolabel="1">
                                    <list>
                                        <field name="name"/>
                                        <field name="renewal_date"/>
                                        <field name="new_end_date"/>
                                        <field name="amount" widget="monetary"/>
                                        <field name="state"/>
                                    </list>
                                </field>
                            </page>
                            
                            <page string="Sales &amp; Invoice" name="sales" 
                                  groups="sales_team.group_sale_salesman">
                                <group>
                                    <group>
                                        <field name="sale_order_id" readonly="True"/>
                                        <field name="sale_order_line_id" readonly="True"/>
                                    </group>
                                    <group>
                                        <field name="invoice_id" readonly="True"/>
                                        <field name="invoice_line_id" readonly="True"/>
                                    </group>
                                </group>
                            </page>
                            
                            <page string="Notes &amp; Tags" name="notes">
                                <group>
                                    <field name="tags" widget="many2many_tags"/>
                                </group>
                                <separator string="Internal Notes"/>
                                <field name="notes" placeholder="Internal notes about this membership..." nolabel="1"/>
                            </page>
                        </notebook>
                    </sheet>
                    
                    <div class="oe_chatter">
                        <chatter/>
                    </div>
                </form>
            </field>
        </record>

        <!-- AMS Membership Kanban View - Enhanced with Chapter Support -->
        <record id="view_ams_membership_kanban" model="ir.ui.view">
            <field name="name">ams.membership.kanban</field>
            <field name="model">ams.membership</field>
            <field name="arch" type="xml">
                <kanban class="o_kanban_mobile" default_group_by="state">
                    <field name="partner_id"/>
                    <field name="product_id"/>
                    <field name="start_date"/>
                    <field name="end_date"/>
                    <field name="state"/>
                    <field name="membership_fee"/>
                    <field name="days_until_expiry"/>
                    <field name="auto_renew"/>
                    <field name="is_chapter_membership"/>
                    <field name="membership_type"/>
                    <field name="chapter_type"/>
                    <field name="chapter_location"/>
                    <field name="chapter_role"/>
                    <field name="chapter_engagement_score"/>
                    <field name="chapter_events_attended"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div t-attf-class="oe_kanban_card oe_kanban_global_click #{record.is_chapter_membership.raw_value ? 'border-info' : ''}">
                                <div class="row">
                                    <div class="col-8">
                                        <strong><t t-esc="record.partner_id.value"/></strong>
                                    </div>
                                    <div class="col-4 text-right">
                                        <span t-attf-class="badge badge-pill badge-#{record.state.raw_value === 'active' ? 'success' : record.state.raw_value === 'grace' ? 'warning' : record.state.raw_value === 'terminated' ? 'danger' : 'secondary'}">
                                            <t t-esc="record.state.value"/>
                                        </span>
                                    </div>
                                </div>
                                <div class="row">
                                    <div class="col-12">
                                        <t t-esc="record.product_id.value"/>
                                        <t t-if="record.is_chapter_membership.raw_value">
                                            <span class="badge badge-info badge-sm ms-1">
                                                <i class="fa fa-map-marker"/> Chapter
                                            </span>
                                        </t>
                                    </div>
                                </div>
                                
                                <!-- Enhanced Chapter-specific info -->
                                <div class="row" t-if="record.is_chapter_membership.raw_value">
                                    <div class="col-12">
                                        <small class="text-muted">
                                            <i class="fa fa-map-marker"/> 
                                            <t t-esc="record.chapter_type.value and record.chapter_type.value.replace('_', ' ').title()"/>
                                            <t t-if="record.chapter_location.value">
                                                - <t t-esc="record.chapter_location.value"/>
                                            </t>
                                        </small>
                                    </div>
                                </div>
                                
                                <!-- Chapter role and engagement -->
                                <div class="row" t-if="record.is_chapter_membership.raw_value and record.chapter_role.value != 'member'">
                                    <div class="col-12">
                                        <small class="text-info">
                                            <i class="fa fa-star"/> 
                                            <t t-esc="record.chapter_role.value and record.chapter_role.value.replace('_', ' ').title()"/>
                                        </small>
                                    </div>
                                </div>
                                
                                <div class="row">
                                    <div class="col-6">
                                        <small>Start: <t t-esc="record.start_date.value"/></small>
                                    </div>
                                    <div class="col-6">
                                        <small>End: <t t-esc="record.end_date.value"/></small>
                                    </div>
                                </div>
                                
                                <div class="row">
                                    <div class="col-6">
                                        <small>$<t t-esc="record.membership_fee.value"/></small>
                                    </div>
                                    <div class="col-6 text-right" t-if="record.days_until_expiry.raw_value != 0">
                                        <small t-attf-class="text-#{record.days_until_expiry.raw_value &lt; 30 ? 'danger' : 'muted'}">
                                            <t t-esc="record.days_until_expiry.value"/> days
                                        </small>
                                    </div>
                                </div>
                                
                                <!-- Chapter engagement indicator -->
                                <div class="row" t-if="record.is_chapter_membership.raw_value and record.chapter_engagement_score.raw_value > 0">
                                    <div class="col-6">
                                        <small class="text-muted">
                                            Engagement: <t t-esc="record.chapter_engagement_score.value"/>%
                                        </small>
                                    </div>
                                    <div class="col-6 text-right" t-if="record.chapter_events_attended.raw_value > 0">
                                        <small class="text-muted">
                                            <i class="fa fa-calendar"/> <t t-esc="record.chapter_events_attended.value"/>
                                        </small>
                                    </div>
                                </div>
                                
                                <div class="row">
                                    <div class="col-12">
                                        <t t-if="record.auto_renew.raw_value">
                                            <span class="badge badge-info">Auto Renew</span>
                                        </t>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <!-- Enhanced Search View with Comprehensive Chapter Filters -->
        <record id="view_ams_membership_search" model="ir.ui.view">
            <field name="name">ams.membership.search</field>
            <field name="model">ams.membership</field>
            <field name="arch" type="xml">
                <search string="Memberships">
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="product_id"/>
                    <field name="member_type_id"/>
                    <field name="chapter_location"/>
                    <field name="chapter_role"/>
                    <separator/>
                    <filter string="Active" name="active" 
                            domain="[('state', '=', 'active')]"/>
                    <filter string="Grace Period" name="grace" 
                            domain="[('state', '=', 'grace')]"/>
                    <filter string="Draft" name="draft" 
                            domain="[('state', '=', 'draft')]"/>
                    <filter string="Suspended" name="suspended" 
                            domain="[('state', '=', 'suspended')]"/>
                    <filter string="Terminated" name="terminated" 
                            domain="[('state', '=', 'terminated')]"/>
                    <separator/>
                    <!-- Membership Type Filters -->
                    <filter string="Regular Memberships" name="regular_memberships" 
                            domain="[('is_chapter_membership', '=', False)]"/>
                    <filter string="Chapter Memberships" name="chapter_memberships" 
                            domain="[('is_chapter_membership', '=', True)]"/>
                    <separator/>
                    <!-- Chapter Type Filters -->
                    <filter string="Local Chapters" name="local_chapters" 
                            domain="[('chapter_type', '=', 'local')]"/>
                    <filter string="Regional Chapters" name="regional_chapters" 
                            domain="[('chapter_type', '=', 'regional')]"/>
                    <filter string="State Chapters" name="state_chapters" 
                            domain="[('chapter_type', '=', 'state')]"/>
                    <filter string="National Chapters" name="national_chapters" 
                            domain="[('chapter_type', '=', 'national')]"/>
                    <filter string="Special Interest Chapters" name="special_chapters" 
                            domain="[('chapter_type', '=', 'special')]"/>
                    <filter string="Student Chapters" name="student_chapters" 
                            domain="[('chapter_type', '=', 'student')]"/>
                    <filter string="Professional Chapters" name="professional_chapters" 
                            domain="[('chapter_type', '=', 'professional')]"/>
                    <separator/>
                    <!-- Chapter Access Level Filters -->
                    <filter string="Basic Access" name="basic_access" 
                            domain="[('chapter_access_level', '=', 'basic')]"/>
                    <filter string="Premium Access" name="premium_access" 
                            domain="[('chapter_access_level', '=', 'premium')]"/>
                    <filter string="Leadership Access" name="leadership_access" 
                            domain="[('chapter_access_level', '=', 'leadership')]"/>
                    <filter string="Officer Access" name="officer_access" 
                            domain="[('chapter_access_level', '=', 'officer')]"/>
                    <separator/>
                    <!-- Chapter Role Filters -->
                    <filter string="Chapter Officers" name="chapter_officers" 
                            domain="[('chapter_role', 'in', ['officer', 'board_member', 'president', 'vice_president', 'secretary', 'treasurer'])]"/>
                    <filter string="Chapter Presidents" name="chapter_presidents" 
                            domain="[('chapter_role', '=', 'president')]"/>
                    <filter string="Committee Members" name="committee_members" 
                            domain="[('chapter_role', '=', 'committee_member')]"/>
                    <filter string="Volunteers" name="volunteers" 
                            domain="[('chapter_role', '=', 'volunteer')]"/>
                    <separator/>
                    <!-- Engagement Filters -->
                    <filter string="High Engagement" name="high_engagement" 
                            domain="[('chapter_engagement_score', '>=', 75)]"/>
                    <filter string="Medium Engagement" name="medium_engagement" 
                            domain="[('chapter_engagement_score', '>=', 50), ('chapter_engagement_score', '&lt;', 75)]"/>
                    <filter string="Low Engagement" name="low_engagement" 
                            domain="[('chapter_engagement_score', '&lt;', 50)]"/>
                    <separator/>
                    <!-- Activity Filters -->
                    <filter string="Recent Activity" name="recent_activity" 
                            domain="[('chapter_last_activity_date', '>=', (context_today() - datetime.timedelta(days=30)).strftime('%Y-%m-%d'))]"/>
                    <filter string="Event Attendees" name="event_attendees" 
                            domain="[('chapter_events_attended', '>', 0)]"/>
                    <filter string="Volunteers" name="has_volunteer_hours" 
                            domain="[('chapter_volunteer_hours', '>', 0)]"/>
                    <separator/>
                    <!-- Standard Filters -->
                    <filter string="Auto Renew" name="auto_renew" 
                            domain="[('auto_renew', '=', True)]"/>
                    <filter string="Manual Renew" name="manual_renew" 
                            domain="[('auto_renew', '=', False)]"/>
                    <separator/>
                    <filter string="Expiring Soon" name="expiring_soon" 
                            domain="[('end_date', '&gt;=', (context_today()).strftime('%Y-%m-%d')), 
                            ('end_date', '&lt;=', (context_today() + datetime.timedelta(days=30)).strftime('%Y-%m-%d'))]"/>
                    <filter string="Expired" name="expired" 
                            domain="[('end_date', '&lt;', (context_today()).strftime('%Y-%m-%d'))]"/>
                    <filter string="Paid" name="paid" 
                            domain="[('payment_status', '=', 'paid')]"/>
                    <filter string="Pending Payment" name="pending_payment" 
                            domain="[('payment_status', '=', 'pending')]"/>
                    <separator/>
                    <filter string="This Year" name="this_year" 
                            domain="[('start_date', '&gt;=', (context_today() - datetime.timedelta(days=365)).strftime('%Y-%m-%d'))]"/>
                    <group expand="0" string="Group By">
                        <filter string="Status" name="group_state" 
                                domain="[]" context="{'group_by': 'state'}"/>
                        <filter string="Member Type" name="group_member_type" 
                                domain="[]" context="{'group_by': 'member_type_id'}"/>
                        <filter string="Product" name="group_product" 
                                domain="[]" context="{'group_by': 'product_id'}"/>
                        <filter string="Membership Type" name="group_membership_type" 
                                domain="[]" context="{'group_by': 'membership_type'}"/>
                        <separator/>
                        <!-- Chapter Groupings -->
                        <filter string="Chapter Type" name="group_chapter_type" 
                                domain="[]" context="{'group_by': 'chapter_type'}"/>
                        <filter string="Chapter Location" name="group_chapter_location" 
                                domain="[]" context="{'group_by': 'chapter_location'}"/>
                        <filter string="Chapter Access Level" name="group_chapter_access_level" 
                                domain="[]" context="{'group_by': 'chapter_access_level'}"/>
                        <filter string="Chapter Role" name="group_chapter_role" 
                                domain="[]" context="{'group_by': 'chapter_role'}"/>
                        <separator/>
                        <filter string="Start Date" name="group_start_date" 
                                domain="[]" context="{'group_by': 'start_date:month'}"/>
                        <filter string="Payment Status" name="group_payment" 
                                domain="[]" context="{'group_by': 'payment_status'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- Rest of existing action records remain the same... -->
        <!-- AMS Membership Action -->
        <record id="action_ams_membership" model="ir.actions.act_window">
            <field name="name">Memberships</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ams.membership</field>
            <field name="view_mode">kanban,list,form</field>
            <field name="context">{'search_default_active': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create your first membership
                </p>
                <p>
                    Memberships are individual membership records that track a member's
                    subscription to the organization. Each member can only have one 
                    active regular membership at a time, but unlimited chapter memberships.
                </p>
            </field>
        </record>

        <!-- Active Memberships Action -->
        <record id="action_ams_membership_active" model="ir.actions.act_window">
            <field name="name">Active Memberships</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ams.membership</field>
            <field name="view_mode">list,kanban,form</field>
            <field name="domain">[('state', '=', 'active')]</field>
            <field name="context">{}</field>
        </record>

        <!-- Expiring Memberships Action -->
        <record id="action_ams_membership_expiring" model="ir.actions.act_window">
            <field name="name">Expiring Memberships</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ams.membership</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('days_until_expiry', '&lt;=', 60), ('days_until_expiry', '&gt;', 0), ('state', '=', 'active')]</field>
            <field name="context">{'search_default_expiring_soon': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No memberships expiring soon
                </p>
                <p>
                    This list shows memberships that expire within the next 60 days
                    and may need renewal reminders.
                </p>
            </field>
        </record>

        <!-- Enhanced Chapter Membership Actions -->
        
        <!-- Active Chapter Memberships Action -->
        <record id="action_ams_membership_chapters_active" model="ir.actions.act_window">
            <field name="name">Active Chapter Memberships</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ams.membership</field>
            <field name="view_mode">kanban,list,form</field>
            <field name="domain">[('is_chapter_membership', '=', True), ('state', '=', 'active')]</field>
            <field name="context">{'search_default_active': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No active chapter memberships found
                </p>
                <p>
                    Chapter memberships provide access to local or regional chapter
                    activities. Members can have multiple chapter memberships.
                </p>
            </field>
        </record>

        <!-- All Chapter Memberships Action -->
        <record id="action_ams_membership_chapters_all" model="ir.actions.act_window">
            <field name="name">All Chapter Memberships</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ams.membership</field>
            <field name="view_mode">list,kanban,form</field>
            <field name="domain">[('is_chapter_membership', '=', True)]</field>
            <field name="context">{}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No chapter memberships found
                </p>
                <p>
                    This view shows all chapter memberships regardless of status.
                    Use filters to narrow down to specific states or chapters.
                </p>
            </field>
        </record>

        <!-- Regular Memberships Only Action -->
        <record id="action_ams_membership_regular_only" model="ir.actions.act_window">
            <field name="name">Regular Memberships</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ams.membership</field>
            <field name="view_mode">kanban,list,form</field>
            <field name="domain">[('is_chapter_membership', '=', False)]</field>
            <field name="context">{'search_default_active': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No regular memberships found
                </p>
                <p>
                    This view shows only regular (non-chapter) memberships.
                    Each member can have only one active regular membership.
                </p>
            </field>
        </record>

        <!-- Chapter Officer Management Action -->
        <record id="action_ams_membership_chapter_officers" model="ir.actions.act_window">
            <field name="name">Chapter Officers</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ams.membership</field>
            <field name="view_mode">list,kanban,form</field>
            <field name="domain">[('is_chapter_membership', '=', True), 
                               ('chapter_role', 'in', ['officer', 'board_member', 'president', 'vice_president', 'secretary', 'treasurer']),
                               ('state', '=', 'active')]</field>
            <field name="context">{'search_default_group_chapter_location': 1, 'search_default_group_chapter_role': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No chapter officers found
                </p>
                <p>
                    This view shows active chapter officers and board members
                    across all chapters.
                </p>
            </field>
        </record>

        <!-- High Engagement Chapter Members -->
        <record id="action_ams_membership_high_engagement" model="ir.actions.act_window">
            <field name="name">High Engagement Chapter Members</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ams.membership</field>
            <field name="view_mode">list,kanban,form</field>
            <field name="domain">[('is_chapter_membership', '=', True), 
                               ('chapter_engagement_score', '>=', 75),
                               ('state', '=', 'active')]</field>
            <field name="context">{'search_default_group_chapter_location': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No highly engaged chapter members found
                </p>
                <p>
                    This view shows chapter members with high engagement scores
                    (75% or higher) who are actively participating in chapter activities.
                </p>
            </field>
        </record>

        <!-- Chapter Activities Dashboard Action -->
        <record id="action_ams_membership_chapter_dashboard" model="ir.actions.act_window">
            <field name="name">Chapter Activities Dashboard</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ams.membership</field>
            <field name="view_mode">kanban,list</field>
            <field name="domain">[('is_chapter_membership', '=', True), ('state', '=', 'active')]</field>
            <field name="context">{
                'search_default_group_chapter_type': 1,
                'search_default_recent_activity': 1
            }</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No chapter activity data found
                </p>
                <p>
                    This dashboard provides an overview of chapter membership
                    activities and engagement across all chapters.
                </p>
            </field>
        </record>

    </data>
</odoo>
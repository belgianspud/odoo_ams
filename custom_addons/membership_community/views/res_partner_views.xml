<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- ========================================== -->
        <!-- RES.PARTNER VIEWS (BASE - SIMPLIFIED) -->
        <!-- ========================================== -->

        <!-- Inherit Partner Form View -->
        <record id="view_partner_membership_form" model="ir.ui.view">
            <field name="name">res.partner.membership.form</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                
                <!-- Add Smart Buttons -->
                <xpath expr="//div[@name='button_box']" position="inside">
                    <button name="action_view_memberships" type="object" class="oe_stat_button" icon="fa-id-card" invisible="membership_count == 0">
                        <div class="o_field_widget o_stat_info">
                            <span class="o_stat_value"><field name="active_membership_count"/>/<field name="membership_count"/></span>
                            <span class="o_stat_text">Memberships</span>
                        </div>
                    </button>
                </xpath>

                <!-- Add Membership Status Ribbon -->
                <xpath expr="//sheet" position="before">
                    <widget name="web_ribbon" title="Active Member" bg_color="text-bg-success" invisible="membership_state != 'active'"/>
                    <widget name="web_ribbon" title="Trial Member" bg_color="text-bg-info" invisible="membership_state != 'trial'"/>
                    <widget name="web_ribbon" title="Expired" bg_color="text-bg-danger" invisible="membership_state != 'expired'"/>
                    <widget name="web_ribbon" title="Seat Member" bg_color="text-bg-warning" invisible="not is_seat_member"/>
                </xpath>

                <!-- Add Membership Fields in Main Form -->
                <xpath expr="//field[@name='category_id']" position="after">
                    <field name="member_number" invisible="not is_member" readonly="1"/>
                    <field name="is_member" invisible="not is_member"/>
                    <field name="membership_state" invisible="not is_member"/>
                    <field name="membership_category_id" invisible="not is_member"/>
                    <field name="is_seat_member" invisible="1"/>
                </xpath>

                <!-- Add Membership Page in Notebook -->
                <xpath expr="//notebook" position="inside">
                    <page name="membership" string="Membership" invisible="membership_count == 0">
                        
                        <group name="membership_summary" string="Membership Summary">
                            <group name="status">
                                <field name="member_number" readonly="1"/>
                                <field name="is_member"/>
                                <field name="membership_state"/>
                                <field name="member_since"/>
                                <field name="membership_category_id"/>
                                <field name="member_category_type" readonly="1"/>
                            </group>
                            <group name="dates">
                                <field name="membership_start_date"/>
                                <field name="membership_end_date"/>
                                <field name="days_until_expiry" invisible="membership_state not in ['active', 'trial']"/>
                            </group>
                        </group>

                        <group name="organizational" string="Organizational Membership" 
                               invisible="not parent_organization_id and not child_member_ids">
                            <group name="org_member">
                                <field name="parent_organization_id" invisible="not parent_organization_id"/>
                                <field name="organizational_role" invisible="not parent_organization_id"/>
                                <field name="seat_subscription_id" invisible="not is_seat_member"/>
                                <field name="is_seat_member" invisible="not is_seat_member"/>
                            </group>
                            <group name="org_parent">
                                <div class="alert alert-info" role="alert" invisible="not child_member_ids">
                                    <p class="mb-0">
                                        <i class="fa fa-info-circle"/> 
                                        <strong>Organization Seats:</strong> This organization has 
                                        <strong><field name="child_member_ids" readonly="1" nolabel="1" 
                                                widget="integer"/> seat members</strong>.
                                    </p>
                                </div>
                            </group>
                        </group>

                        <separator string="Organization Seat Members" invisible="not child_member_ids"/>
                        <field name="child_member_ids" nolabel="1" invisible="not child_member_ids">
                            <list>
                                <field name="name"/>
                                <field name="email"/>
                                <field name="organizational_role"/>
                                <field name="membership_state" widget="badge"
                                       decoration-success="membership_state=='active'"
                                       decoration-info="membership_state=='trial'"
                                       decoration-danger="membership_state=='expired'"/>
                                <field name="seat_subscription_id"/>
                            </list>
                        </field>

                        <group name="lifecycle" string="Lifecycle Status" invisible="not is_member">
                            <group>
                                <field name="paid_through_date"/>
                                <field name="grace_period_end_date" invisible="not is_in_grace_period"/>
                                <field name="lifecycle_stage" widget="badge"
                                       decoration-success="lifecycle_stage=='active'"
                                       decoration-warning="lifecycle_stage=='grace'"
                                       decoration-danger="lifecycle_stage in ('suspended','terminated')"/>
                            </group>
                            <group>
                                <field name="days_until_suspension" invisible="not is_in_grace_period"/>
                                <field name="days_until_termination" invisible="lifecycle_stage not in ('grace','suspended')"/>
                            </group>
                        </group>

                        <group name="portal" string="Portal Access">
                            <group>
                                <field name="portal_access_level"/>
                                <field name="has_portal_access"/>
                            </group>
                        </group>

                        <separator string="All Membership Subscriptions"/>
                        <field name="membership_subscription_ids" nolabel="1">
                            <list decoration-success="state=='active'" decoration-info="state=='trial'" decoration-muted="state in ('expired','cancelled')">
                                <field name="name"/>
                                <field name="plan_id"/>
                                <field name="membership_category_id"/>
                                <field name="is_seat_subscription" optional="show"/>
                                <field name="date_start"/>
                                <field name="date_end"/>
                                <field name="state"/>
                                <field name="price" sum="Total"/>
                            </list>
                        </field>

                        <separator string="Membership Invoices"/>
                        <field name="invoice_ids" nolabel="1" 
                               domain="[('move_type', '=', 'out_invoice')]"
                               context="{'default_move_type': 'out_invoice', 'default_partner_id': id}">
                            <list decoration-success="payment_state=='paid'" decoration-info="state=='draft'" decoration-warning="payment_state in ('partial','not_paid')">
                                <field name="name"/>
                                <field name="invoice_date"/>
                                <field name="invoice_date_due"/>
                                <field name="amount_total" sum="Total"/>
                                <field name="payment_state" widget="badge"/>
                                <field name="state" widget="badge"/>
                            </list>
                        </field>

                    </page>

                    <page name="member_features" string="Features &amp; Benefits" invisible="not is_member">
                        
                        <div class="alert alert-success" role="alert" invisible="not is_seat_member">
                            <p class="mb-0">
                                <i class="fa fa-users"/> 
                                <strong>Seat Member:</strong> Benefits inherited from 
                                <field name="parent_organization_id" readonly="1" nolabel="1"/> organization membership.
                            </p>
                        </div>

                        <separator string="Available Features"/>
                        <field name="available_features" nolabel="1" widget="many2many_tags">
                            <list>
                                <field name="name"/>
                                <field name="category"/>
                                <field name="feature_type"/>
                            </list>
                        </field>

                        <separator string="Available Benefits"/>
                        <field name="available_benefits" nolabel="1" widget="many2many_tags">
                            <list>
                                <field name="name"/>
                                <field name="category"/>
                                <field name="benefit_type"/>
                                <field name="monetary_value"/>
                            </list>
                        </field>

                    </page>
                </xpath>

            </field>
        </record>

        <!-- Inherit Partner List View -->
        <record id="view_partner_membership_tree" model="ir.ui.view">
            <field name="name">res.partner.membership.list</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//list" position="inside">
                    <field name="member_number" optional="show"/>
                    <field name="is_member" optional="show"/>
                    <field name="is_seat_member" optional="hide"/>
                    <field name="membership_state" optional="show"/>
                    <field name="membership_category_id" optional="hide"/>
                    <field name="membership_end_date" optional="hide"/>
                    <field name="parent_organization_id" optional="hide"/>
                </xpath>
            </field>
        </record>

        <!-- Inherit Partner Search View -->
        <record id="view_partner_membership_search" model="ir.ui.view">
            <field name="name">res.partner.membership.search</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_res_partner_filter"/>
            <field name="arch" type="xml">
                <xpath expr="//filter[@name='customer']" position="after">
                    <separator/>
                    <filter string="Members" name="members" domain="[('is_member', '=', True)]"/>
                    <filter string="Active Members" name="active_members" domain="[('membership_state', '=', 'active')]"/>
                    <filter string="Trial Members" name="trial_members" domain="[('membership_state', '=', 'trial')]"/>
                    <filter string="Expired Members" name="expired_members" domain="[('membership_state', '=', 'expired')]"/>
                    <separator/>
                    <filter string="Seat Members" name="seat_members" domain="[('is_seat_member', '=', True)]"/>
                    <filter string="Organizations with Seats" name="organizations_with_seats" domain="[('child_member_ids', '!=', False)]"/>
                    <separator/>
                    <filter string="Has Portal Access" name="has_portal" domain="[('has_portal_access', '=', True)]"/>
                </xpath>
                <xpath expr="//group" position="inside">
                    <filter string="Membership State" name="group_membership_state" context="{'group_by': 'membership_state'}"/>
                    <filter string="Member Category" name="group_member_category" context="{'group_by': 'membership_category_id'}"/>
                    <filter string="Parent Organization" name="group_parent_org" context="{'group_by': 'parent_organization_id'}"/>
                </xpath>
            </field>
        </record>

        <!-- Member Directory Action -->
        <record id="action_member_directory" model="ir.actions.act_window">
            <field name="name">Member Directory</field>
            <field name="res_model">res.partner</field>
            <field name="view_mode">kanban,list,form</field>
            <field name="domain">[('is_member', '=', True)]</field>
            <field name="context">{'search_default_active_members': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No members found
                </p>
                <p>
                    The member directory shows all active members of your organization.
                </p>
            </field>
        </record>

        <!-- Kanban View for Member Directory -->
        <record id="view_partner_member_directory_kanban" model="ir.ui.view">
            <field name="name">res.partner.member.directory.kanban</field>
            <field name="model">res.partner</field>
            <field name="arch" type="xml">
                <kanban class="o_kanban_mobile">
                    <field name="name"/>
                    <field name="email"/>
                    <field name="phone"/>
                    <field name="member_number"/>
                    <field name="is_member"/>
                    <field name="is_seat_member"/>
                    <field name="membership_state"/>
                    <field name="membership_category_id"/>
                    <field name="member_since"/>
                    <field name="parent_organization_id"/>
                    <field name="avatar_128"/>
                    <templates>
                        <t t-name="kanban-box">
                            <div class="oe_kanban_global_click o_kanban_record_has_image_fill">
                                <div class="o_kanban_image_fill_left d-none d-md-block" t-attf-style="background-image: url('#{kanban_image('res.partner', 'avatar_128', record.id.raw_value)}')"/>
                                <div class="oe_kanban_details">
                                    <div class="o_kanban_record_top">
                                        <div class="o_kanban_record_headings">
                                            <strong class="o_kanban_record_title">
                                                <field name="name"/>
                                            </strong>
                                            <div t-if="record.member_number.raw_value" class="text-muted">
                                                <field name="member_number"/>
                                            </div>
                                        </div>
                                        <span class="float-end">
                                            <field name="membership_state" widget="badge" 
                                                   decoration-success="membership_state=='active'"
                                                   decoration-info="membership_state=='trial'"
                                                   decoration-danger="membership_state=='expired'"/>
                                            <span t-if="record.is_seat_member.raw_value" class="badge text-bg-warning ms-1">
                                                <i class="fa fa-users"/> Seat
                                            </span>
                                        </span>
                                    </div>
                                    <div class="o_kanban_record_body">
                                        <field name="membership_category_id"/>
                                        <div t-if="record.parent_organization_id.raw_value" class="text-muted">
                                            <i class="fa fa-building"/> <field name="parent_organization_id"/>
                                        </div>
                                        <div t-if="record.member_since.raw_value">
                                            <i class="fa fa-calendar"/> Member since <field name="member_since"/>
                                        </div>
                                        <div t-if="record.email.raw_value">
                                            <i class="fa fa-envelope"/> <field name="email"/>
                                        </div>
                                        <div t-if="record.phone.raw_value">
                                            <i class="fa fa-phone"/> <field name="phone"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

    </data>
</odoo>
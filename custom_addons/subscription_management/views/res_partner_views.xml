<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Partner Form View - Inherit (Updated ID to force refresh) -->
    <record id="res_partner_form_view_inherit_subscription_v2" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.subscription.v2</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            
            <!-- Add smart button -->
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_subscriptions" 
                        type="object" 
                        class="oe_stat_button" 
                        icon="fa-refresh"
                        invisible="subscription_count == 0">
                    <field name="subscription_count" widget="statinfo" string="Subscriptions"/>
                </button>
            </xpath>
            
            <!-- Add hidden fields for counts -->
            <xpath expr="//field[@name='category_id']" position="after">
                <field name="active_subscription_count" invisible="1"/>
            </xpath>
            
        </field>
    </record>

</odoo>
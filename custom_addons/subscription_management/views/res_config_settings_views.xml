<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Inherit Settings Form -->
    <record id="res_config_settings_view_form_inherit_subscription" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.subscription</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app string="Subscriptions" name="subscription_management" groups="subscription_management.group_subscription_manager">
                    
                    <block title="Subscription Lifecycle Management" name="subscription_lifecycle">
                        
                        <setting id="subscription_grace_period" string="Grace Period">
                            <div class="content-group">
                                <div class="row mt16">
                                    <label for="subscription_grace_period_days" class="col-lg-3 o_light_label"/>
                                    <field name="subscription_grace_period_days"/>
                                </div>
                                <div class="text-muted">
                                    Number of days after expiry before suspension. Subscription retains full access during grace period.
                                </div>
                                <div class="mt16">
                                    <field name="subscription_grace_send_email" class="o_light_label"/>
                                    <label for="subscription_grace_send_email"/>
                                </div>
                                <div class="mt8" invisible="not subscription_grace_send_email">
                                    <label for="subscription_grace_email_frequency" class="o_light_label"/>
                                    <field name="subscription_grace_email_frequency"/>
                                </div>
                            </div>
                        </setting>
                        
                        <setting id="subscription_suspend_period" string="Suspension Period">
                            <div class="content-group">
                                <div class="row mt16">
                                    <label for="subscription_suspend_period_days" class="col-lg-3 o_light_label"/>
                                    <field name="subscription_suspend_period_days"/>
                                </div>
                                <div class="text-muted">
                                    Number of days in suspension before termination. Subscription has limited access during suspension.
                                </div>
                                <div class="mt16">
                                    <field name="subscription_suspend_restrict_portal" class="o_light_label"/>
                                    <label for="subscription_suspend_restrict_portal"/>
                                </div>
                                <div class="mt8">
                                    <field name="subscription_suspend_send_email" class="o_light_label"/>
                                    <label for="subscription_suspend_send_email"/>
                                </div>
                            </div>
                        </setting>
                        
                        <setting id="subscription_terminate_period" string="Termination Period">
                            <div class="content-group">
                                <div class="row mt16">
                                    <label for="subscription_terminate_period_days" class="col-lg-3 o_light_label"/>
                                    <field name="subscription_terminate_period_days"/>
                                </div>
                                <div class="text-muted">
                                    Total days before final termination. After this period, subscription is permanently terminated.
                                </div>
                                <div class="alert alert-info mt16" role="alert">
                                    <strong>Timeline:</strong> After expiry, subscriptions go through:
                                    <ul class="mb-0 mt-2">
                                        <li>Grace Period (full access with warnings)</li>
                                        <li>Suspension (limited access)</li>
                                        <li>Termination (no access, archived)</li>
                                    </ul>
                                </div>
                            </div>
                        </setting>
                        
                    </block>
                </app>
            </xpath>
        </field>
    </record>

</odoo>
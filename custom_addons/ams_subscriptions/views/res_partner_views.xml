<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Partner Form View Extension -->
    <record id="view_partner_form_ams" model="ir.ui.view">
        <field name="name">res.partner.form.ams</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <page name="sales_purchases" position="after">
                <page string="AMS Memberships" invisible="not is_ams_member">
                    <group>
                        <group>
                            <field name="is_ams_member"/>
                            <field name="member_status"/>
                            <field name="primary_membership_id"/>
                        </group>
                        <group>
                            <field name="active_subscription_count"/>
                            <field name="expired_subscription_count"/>
                        </group>
                    </group>
                    <field name="ams_subscription_ids" readonly="1">
                        <tree>
                            <field name="product_id"/>
                            <field name="subscription_type"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="state"/>
                        </tree>
                    </field>
                </page>
            </page>
            <field name="is_company" position="after">
                <field name="is_ams_member"/>
            </field>
        </field>
    </record>

</odoo>